services:
  bot:
    build: .
    env_file:
      - .env.dev
    volumes:
      - ./src:/app/src
      - ./alembic:/app/alembic
      - bot-data:/app/data
    restart: unless-stopped

volumes:
  bot-data:
